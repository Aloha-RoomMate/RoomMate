rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 로그인한 사용자만 모든 문서에 대한 읽기, 쓰기를 허용합니다.
      allow read, write: if request.auth != null;
    }
  }
}